FROM node:20-alpine

WORKDIR /scr

# Install the application dependencies
COPY package*.json ./

RUN npm install

# Copy in the source code
COPY . .
EXPOSE 5173


# Add host flag so Vite accepts connections from outside container
CMD ["npm", "run", "dev", "--", "--host"]